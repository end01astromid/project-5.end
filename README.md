ToDo + Авторизация (Node.js + Express + MongoDB)

Это простой проект, где есть регистрация, вход по JWT и список задач (ToDo List).
Каждый пользователь видит только свои задачи.

Что делает проект?
Можно зарегистрироваться
Можно войти, чтобы получить токен (JWT)
Можно создавать, смотреть, редактировать и удалять свои задачи

Что используется?
Node.js — для сервера
Express — для маршрутов
MongoDB + Mongoose — база данных
bcrypt — шифрование пароля
jsonwebtoken (JWT) — защита маршрутов

1Установка
Склонируй проект или создай папку и вставь код

2 Установи зависимости
npm install express mongoose bcrypt jsonwebtoken dotenv

3 Создай файл .env и добавь туда:
MONGO_URI=твой_адрес_монго
JWT_SECRET=любой_секретный_ключ
PORT=5000

4 Запусти сервер
node index.js тд.....



🧠 Как это работает целом код?
Когда ты регистрируешься, твой пароль шифруется и сохраняется в базе.
Когда входишь, сервер проверяет пароль и создаёт токен JWT.
Этот токен отправляется с каждым запросом (в Authorization).
Сервер через authMiddleware проверяет токен и узнаёт, кто ты.
Задачи (Task) сохраняются с полем user, чтобы знать, кому они принадлежат.
Поэтому каждый пользователь видит только свои задачи.
