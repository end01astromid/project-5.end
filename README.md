ToDo + Авторизация + Отправка писем (Node.js + Express + MongoDB)

Это простой проект, где можно зарегистрироваться, войти через JWT-токен, управлять своими задачами (ToDo List) и при регистрации отправлять письмо на почту.
Каждый пользователь видит только свои задачи.

Что делает проект?
🔹 Регистрация с письмом на email
🔹 Вход (логин) по email и паролю
🔹 Получение токена (JWT)
🔹 Создание, просмотр, редактирование и удаление задач
🔹 Только авторизованные пользователи могут работать со своими задачами

Что используется?
Node.js — сервер
Express — маршруты
MongoDB + Mongoose — база данных
bcrypt — шифрование паролей
jsonwebtoken (JWT) — токен для авторизации
dotenv — хранение секретных данных
nodemailer — отправка писем на почту


🧠 Как это работает простыми словами

При регистрации сервер сохраняет пользователя и отправляет ему письмо с подтверждением.
При входе сервер проверяет пароль и создаёт токен (JWT).
Этот токен добавляется в заголовок Authorization.
Через authMiddleware сервер проверяет токен и знает, кто делает запрос.
При создании задач сервер добавляет в них ID пользователя, чтобы каждый видел только свои.
